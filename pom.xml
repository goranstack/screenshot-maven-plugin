<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.sonatype.oss</groupId>
		<artifactId>oss-parent</artifactId>
		<version>9</version>
	</parent>
	<groupId>se.bluebrim.maven.plugin</groupId>
	<artifactId>screenshot-maven-plugin-parent</artifactId>
	<version>0.0.8-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>Screenshot Maven Plugin Parent</name>
	<description>Parent project to screenshot-maven-plugin and additional modules</description>
	<url>https://github.com/goranstack/screenshot-maven-plugin</url>
	<prerequisites>
		<maven>3.0</maven>
	</prerequisites>
	<licenses>
		<license>
			<name>Apache License, Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0</url>
		</license>
	</licenses>
	<scm>
		<connection>scm:git:git@github.com:goranstack/screenshot-maven-plugin.git</connection>
		<developerConnection>scm:git:git@github.com:goranstack/screenshot-maven-plugin.git</developerConnection>
		<url>scm:git:https://github.com/goranstack/screenshot-maven-plugin</url>
	</scm>
	<properties>
		<screenshot-maven-plugin-version>${project.version}</screenshot-maven-plugin-version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<repo.release.id>git-maven-repo-relases</repo.release.id>
		<repo.release.url>git:https://github.com/goranstack/screenshot-maven-plugin/tree/master/mavenrepo</repo.release.url>
		<repo.snapshot.id>git-maven-repo-snapshots</repo.snapshot.id>
		<repo.snapshot.url>git:https://github.com/goranstack/screenshot-maven-plugin/tree/master/mavenrepo-snapshot</repo.snapshot.url>
		<rootDirectory>${project.basedir}</rootDirectory>
		<screenshotGalleryTarget>${rootDirectory}/gh-pages/target/gallery-src</screenshotGalleryTarget>
	</properties>
	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
			</resource>
			<!-- Make it possible to put resource files along side the Java classes -->
			<resource>
				<directory>src/main/java</directory>
				<excludes>
					<exclude>**/*.java</exclude>
				</excludes>
			</resource>
		</resources>
		<testResources>
			<testResource>
				<directory>src/test/resources</directory>
			</testResource>
			<testResource>
				<directory>src/test/java</directory>
				<excludes>
					<exclude>**/*.java</exclude>
				</excludes>
			</testResource>
		</testResources>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-release-plugin</artifactId>
					<version>2.5.3</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-plugin-plugin</artifactId>
					<version>3.5</version>
				</plugin>
				<plugin>
					<groupId>se.bluebrim.maven.plugin</groupId>
					<artifactId>screenshot-maven-plugin</artifactId>
					<version>${screenshot-maven-plugin-version}</version>
					<configuration>
						<goalPrefix>screenshot</goalPrefix>
						<!-- Can't use ${project.scm.url} because that is appended with artifactId 
							of the module. See: http://jira.codehaus.org/browse/MNG-2290 -->
						<sourceCodeURL>https://github.com/goranstack/screenshot-maven-plugin/tree/master/${project.artifactId}/src/main/java</sourceCodeURL>
						<javaDocImageScale>0.6</javaDocImageScale>
						<outputDirectory>${screenshotGalleryTarget}</outputDirectory>
						<locales>
							<localeSpec>
								<language>en</language>
								<country>US</country>
							</localeSpec>
						</locales>
					</configuration>
					<executions>
						<execution>
							<id>create-gallery</id>
							<phase>none</phase>
							<goals>
								<goal>gallery</goal>
							</goals>
							<configuration>
								<outputDirectory>${screenshotGalleryTarget}</outputDirectory>
							</configuration>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement><plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.5.1</version>
				<configuration>
					<source>1.8</source>
					<target>1.8</target>
					<encoding>UTF-8</encoding>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>3.0.1</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<phase>verify</phase>
						<goals>
							<goal>jar-no-fork</goal>
							<goal>test-jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.0.2</version>
				<!-- Make it possible for test code to depend on test code in other modules -->
				<executions>
					<execution>
						<goals>
							<goal>test-jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-site-plugin</artifactId>
				<version>3.7</version>
				<executions>
					<execution>
						<id>createsite</id>
						<phase>package</phase>
						<goals>
							<goal>site</goal>
						</goals>
						<configuration>
							<reportPlugins>
								<plugin>
									<groupId>org.apache.maven.plugins</groupId>
									<artifactId>maven-project-info-reports-plugin</artifactId>
									<version>2.9</version>
									<reportSets>
										<reportSet>
											<reports>
												<report>plugin-info</report>
											</reports>
										</reportSet>
									</reportSets>
								</plugin>
							</reportPlugins>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>		
	</build>

	<!-- Found at Don Brown's Weblog: http://www.jroller.com/mrdon/entry/find_of_the_day_wagon -->
	<distributionManagement>
		<repository>
			<uniqueVersion>false</uniqueVersion>
			<id>${repo.release.id}</id>
			<url>${repo.release.url}</url>
		</repository>
		<snapshotRepository>
			<uniqueVersion>false</uniqueVersion>
			<id>${repo.snapshot.id}</id>
			<url>${repo.snapshot.url}</url>
		</snapshotRepository>
	</distributionManagement>
	<profiles>
		<profile>
			<id>atg-private-maven-repo</id>
			<properties>
				<repo.release.id>atg-releases</repo.release.id>
				<repo.release.url>http://nexus.hastsportenshus.atg.se/content/repositories/releases</repo.release.url>
				<repo.snapshot.id>atg-snapshots</repo.snapshot.id>
				<repo.snapshot.url>http://nexus.hastsportenshus.atg.se/content/repositories/snapshots</repo.snapshot.url>
			</properties>
		</profile>
	</profiles>

	<modules>
		<module>screenshot-maven-plugin-api</module>
		<module>screenshot-maven-plugin</module>
		<module>gh-pages</module>
		<module>swingset3-demo-app</module>
		<module>screenshot-examples</module>
		<module>worldclock-example</module>
	</modules>
</project>